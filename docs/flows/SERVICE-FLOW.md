# Service Management Flow

Create or edit a service with name, description, duration, buffer, price, tax behavior, capacity, branch, eligible staff, booking window and cancellation policy.

Validation must prevent negative duration or price, impossible booking windows, missing tenant ownership and incompatible staff or branch assignments. Publishing makes the service bookable; archiving removes it from new bookings but preserves historical appointments. Changes affecting existing appointments require explicit policy and audit entries.
